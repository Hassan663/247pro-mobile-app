// @app
import React, {
    useState
} from 'react';
import {
    View,
    Image,
    TouchableOpacity,
    ScrollView,
    Dimensions,
} from 'react-native';
const Tasks: React.FC<{ navigation: any, route: any }> = ({ navigation, route }) => {

    return (
        <>


        </>

    );
};

export default Tasks;
