// @app
import React, {
    useState
} from 'react';
import {
    View,
    Image,
    TouchableOpacity,
    ScrollView,
    Dimensions,
} from 'react-native';
const MarketPlace: React.FC<{ navigation: any, route: any }> = ({ navigation, route }) => {

    return (
        <>


        </>

    );
};

export default MarketPlace;
